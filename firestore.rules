rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Client-side direct Firestore access is not used in this project.
    // All reads/writes must go through Cloud Functions.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
