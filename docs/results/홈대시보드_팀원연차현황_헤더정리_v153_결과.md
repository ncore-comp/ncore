# 홈 대시보드 팀원 연차 현황 헤더 정리 v153 결과
# Home Dashboard Member Status Header Cleanup v153 Result

## 요약
## Summary

- `팀원 연차 현황` 카드의 헤더 컨트롤 위치를 정리했다.
- The header controls for the `Team Member Leave Status` cards were reorganized.

- `사용 내역` 토글과 `펼치기/접기` 버튼을 같은 우측 컨트롤 영역으로 묶었다.
- The `Usage History` toggle and the `Expand/Collapse` button are now grouped in the same right-side control area.

- `직원 카드 켜짐/꺼짐` 문구는 제거했다.
- The `Employee Card On/Off` text was removed.

## 반영 내용
## Applied Changes

- 제목은 좌측에 고정하고, 조작 요소는 우측에 정렬했다.
- The title stays on the left, while interactive controls are aligned on the right.

- `펼치기/접기` 버튼은 독립 버튼 형태로 정리했다.
- The `Expand/Collapse` control is now a standalone button.

- `사용 내역` 토글은 기존 방식 유지하되, 같은 우측 묶음 안에서 보이도록 정리했다.
- The `Usage History` toggle keeps its existing behavior but now appears within the same right-side control group.

## 확인 포인트
## Checkpoints

1. `매뉴얼팀`, `파츠북팀` 카드 헤더에서 우측에 `사용 내역` 토글과 `펼치기/접기` 버튼이 함께 보여야 한다.
1. In both `Manual Team` and `Partsbook Team` headers, the `Usage History` toggle and `Expand/Collapse` button should appear together on the right.

2. `직원 카드 켜짐` 문구는 더 이상 보이지 않아야 한다.
2. The `Employee Card On` text should no longer appear.
