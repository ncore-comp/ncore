# 디버그 사용자 삭제 결과 (2026-04-06)
# Debug User Deletion Result (2026-04-06)

## 처리 요약
## Summary

- 운영 `users` 컬렉션에 섞여 있던 `DBG...` 디버그 사용자 6명을 삭제했습니다.
- Removed 6 `DBG...` debug users that had been mixed into the live `users` collection.

## 삭제 대상
## Deleted Targets

1. `DBG_ANNUAL_debug_1775443102050`
2. `DBG2_debug2_1775443210968`
3. `DBG3_debug3_1775443279129`
4. `DBG4_debug4_1775443496910`
5. `DBG5_debug5_1775443593767`
6. `DBG6_debug6_1775443632231`

## 삭제 전 점검
## Pre-delete Checks

- 연결 요청 / Linked requests: `0건`
- 연결 특별휴가 부여 / Linked user special leaves: `0건`
- 접속 로그 / Access logs: `13건`

설명 / Notes:
- 요청과 특별휴가 부여가 없어서 사용자 문서만 삭제했습니다.
- 접속 로그는 감사 추적용으로 유지했습니다.
- Since there were no linked requests or special-leave grants, only user documents were deleted.
- Access logs were retained for audit history.

## 삭제 스크립트
## Deletion Script

- [delete-debug-users-2026-04-06.js](/D:/MYDOC/Desktop/vscode/Web/firebase/ncore_web/ncore-main/scripts/delete-debug-users-2026-04-06.js)

## 검증 결과
## Verification Result

### 소스 파일 수정
### Source Updated

- 완료
- Completed

### 현재 운영 데이터 반영
### Applied to Live Data

- 완료
- Completed

### 사용자 화면 확인
### Confirmed on Visible Screen

- 완료
- Completed

## 검증 상세
## Verification Details

- Firestore 재확인:
  - 남은 `DBG` 사용자 / Remaining DBG users: `0`
  - 남은 `DBG` 요청 / Remaining DBG requests: `0`
  - 남은 `DBG` 특별휴가 부여 / Remaining DBG user special leaves: `0`

- 브라우저 직접 확인:
  - 운영실 대상 직원 드롭다운에서 `DBG` 없음
  - 구성원 관리 목록에서 `DBG` 없음

## 다음 권장 사항
## Recommended Next Step

- 테스트/디버그 사용자 생성 규칙을 `SIM_`, `DBG_` 패턴으로 더 명확히 고정하고, 정리 스크립트를 상시 보관하는 것이 좋습니다.
- It would be safer to standardize test/debug user prefixes such as `SIM_` and `DBG_`, and keep a cleanup script ready for reuse.
