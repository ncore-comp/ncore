> 시간 기록 기준: Asia/Seoul
> 문서 최초 작성 시각(기록 기준): 2026-03-26 11:00:16
> 문서 최근 수정 시각(기록 기준): 2026-03-26 11:00:16

# 권한설정 세부설정 분리 / 모바일 권한 추가 결과

## 작업 결과

- 권한 탭 그룹에 `세부설정`을 추가했다.
- 기존 `권한/설정 접근`, `구성원 관리`를 `세부설정` 그룹 안으로 이동했다.
- 권한을 PC/모바일 기준으로 분리했다.

## 세부설정 항목

- 권한/설정 접근
- 구성원 관리
- 권한/설정 접근(모바일)
- 구성원 관리(모바일)

## 저장 구조 변경

- `canAccessMasterSettingsDesktop`
- `canManageUsersDesktop`
- `canAccessMasterSettingsMobile`
- `canManageUsersMobile`

기존 호환:
- 예전 `canAccessMasterSettings`, `canManageUsers` 값은
  PC/모바일 새 권한이 없을 때 초기값으로 함께 해석되도록 유지했다.

## 접근 판단 변경

- PC 접속:
  - Desktop 권한 사용
- 모바일 접속:
  - Mobile 권한 사용

## 수정 파일

- [D:\MYDOC\Desktop\vscode\Web\firebase\ncore_web\ncore-main\js\ui_logic_new_design_v114_codex.js](/D:/MYDOC/Desktop/vscode/Web/firebase/ncore_web/ncore-main/js/ui_logic_new_design_v114_codex.js)
- [D:\MYDOC\Desktop\vscode\Web\firebase\ncore_web\ncore-main\functions\src\lib\common.js](/D:/MYDOC/Desktop/vscode/Web/firebase/ncore_web/ncore-main/functions/src/lib/common.js)
- [D:\MYDOC\Desktop\vscode\Web\firebase\ncore_web\ncore-main\functions\src\services\userService.js](/D:/MYDOC/Desktop/vscode/Web/firebase/ncore_web/ncore-main/functions/src/services/userService.js)
- [D:\MYDOC\Desktop\vscode\Web\firebase\ncore_web\ncore-main\권한설정_세부설정분리_모바일권한추가_계획.md](/D:/MYDOC/Desktop/vscode/Web/firebase/ncore_web/ncore-main/docs/plans/권한설정_세부설정분리_모바일권한추가_계획.md)

## 검증

- JS 문법 검사 통과
- Functions 문법 검사 통과
- `functions + hosting` 운영 반영 완료
- 운영 웹 스크립트 반영 확인:
  - `세부설정`
  - `perm-master-access-mobile`
  - `canAccessMasterSettingsDesktop`
  - `canManageUsersMobile`

## 배포

- 배포 시각: 2026-03-26 10:58:08
- 대상:
  - [https://ncore.web.app](https://ncore.web.app)
  - [https://ncore-vacation-system.web.app](https://ncore-vacation-system.web.app)
## 2026-03-26 11:11 세부설정 헤더 정리 및 가독성 보강

- `thead`에 남아 있던 옛 `권한/설정 접근`, `구성원 관리` 2컬럼 제거
- 빈 행 안내 `colspan`을 현재 펼친 그룹 너비에 맞게 정리
- `세부설정`을 펼쳤을 때만 4개 컬럼만 보이도록 헤더/본문 구조 일치
- `PC` 2칸 / `모바일` 2칸 사이에 구분선과 색상 차이를 넣어 가독성 보강
- 마스터 행은 세부설정 4칸 모두 체크 상태로 표시되도록 프런트 권한 해석 보강
- `functions + hosting` 운영 반영 완료

## 2026-03-26 11:24 완료 확인

- 사용자 확인 기준으로 세부설정 표 구조와 마스터 체크 상태가 정상 동작하는 것으로 마감

## 2026-03-26 12:02 운영실 권한 분리 및 직접 진입 보정

- 세부설정에 `운영실 접근(PC)`, `운영실 접근(모바일)` 2개 항목 추가
- `구성원 관리` 권한과 `운영실 접근` 권한을 분리
- 따라서 구성원 관리 권한만으로는 더 이상 운영실이 열리지 않도록 조정
- `운영실`로 직접 진입할 때 `loadAdminOpsData()`가 먼저 실행되도록 보정
- 운영실 접근 권한만 있어도 수동 휴일 설정/최근 접속 로그가 마스터와 같은 구조로 바로 로드되도록 보정
- `functions + hosting` 운영 반영 완료

## 2026-03-26 12:12 세부설정 열 폭 압축

- 세부설정 6개 컬럼의 열 폭을 줄여 한 화면에 더 가깝게 들어오도록 압축
- 헤더 패딩과 뱃지 크기를 줄여 가독성을 유지하면서 폭을 절감
- `권한/설정 접근` 헤더는 줄바꿈 형태로 축소
- hosting 운영 반영 완료

## 2026-03-26 12:27 전체 상황판 권한 그룹 추가

- 권한 그룹 순서를 `달력정보 / 승인권한 / 팀원 연차현황 / 전체 상황판 / 세부설정`으로 확장
- `전체 상황판` 그룹에 `전체 상황판 접근(PC)`, `전체 상황판 접근(모바일)` 2개 권한 추가
- `canAccessSituationBoardDesktop`, `canAccessSituationBoardMobile` 저장/해석 로직 추가
- 상단 `전체 상황판` 버튼 노출 및 진입 판정을 새 권한 기준으로 변경
- `functions + hosting` 운영 반영 완료

## 2026-03-26 12:51 대표 전체 상황판 권한 강제 규칙 제거

- 대표(`ceo`) 계정에 전체 상황판 권한을 자동으로 다시 켜던 기본 규칙 제거
- 이제 대표도 `전체 상황판 접근(PC/모바일)` 체크박스 값대로 저장/표시
- 저장 후 원상복구되던 문제 해소
- `functions + hosting` 운영 반영 완료

