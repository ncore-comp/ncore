# 시간차(외출) UI 옵션 제한 v130 결과
# Time-Off (Outing) UI Option Restriction v130 Result

## 목적
## Purpose

- `시간차(외출)` 시작시간 드롭다운이 대상 직원의 근무시간 기준으로만 보이도록 맞춥니다.
- Restrict the `시간차(외출)` start-time dropdown so it only reflects the selected employee's work shift.

## 이번 단계에서 한 일
## What Was Done in This Step

- `req-start-time-out` 옵션을 고정 시간표가 아니라 대상 직원 + 선택 날짜 + 사용시간 기준으로 다시 계산하도록 정리했습니다.
- Updated `req-start-time-out` so it is recalculated from the selected employee + selected date + selected duration, instead of using a fixed shared timetable.

- `시간차(외출)`으로 종류를 바꾸는 순간에도 옵션이 바로 다시 계산되도록 초기화 순서를 보강했습니다.
- Strengthened initialization so the options are recomputed immediately when switching the request type to `시간차(외출)`.

## 수정 파일
## Updated Files

- [index.html](/D:/MYDOC/Desktop/vscode/Web/firebase/ncore_web/ncore-main/index.html)
- [ui_logic_new_design_v130_codex.js](/D:/MYDOC/Desktop/vscode/Web/firebase/ncore_web/ncore-main/js/ui_logic_new_design_v130_codex.js)
- [ui_shared_helpers_v130_codex.js](/D:/MYDOC/Desktop/vscode/Web/firebase/ncore_web/ncore-main/js/ui_shared_helpers_v130_codex.js)

## 검증 결과
## Verification Result

### 박서진 (`09:00 ~ 18:00`) / 사용시간 1시간
### Park Seo-jin (`09:00 ~ 18:00`) / Duration 1 hour

- 시작시간 옵션:
- Start-time options:
  - `09:00`
  - `10:00`
  - `11:00`
  - `12:00`
  - `13:00`
  - `14:00`
  - `15:00`
  - `16:00`

- 제외된 값:
- Excluded values:
  - `07:00`
  - `08:00`
  - `17:00`

### 박서진 (`09:00 ~ 18:00`) / 사용시간 2시간
### Park Seo-jin (`09:00 ~ 18:00`) / Duration 2 hours

- 시작시간 옵션:
- Start-time options:
  - `09:00`
  - `10:00`
  - `11:00`
  - `12:00`
  - `13:00`
  - `14:00`
  - `15:00`

- 제외된 값:
- Excluded values:
  - `16:00`
  - `17:00`

## 상태 구분
## Status Breakdown

- 소스 파일 수정
- Source files updated
  - 완료
  - Done

- 현재 운영 웹 반영
- Applied to current live web
  - 완료
  - Done

- 사용자 화면에서 확인
- Confirmed on the user's visible screen
  - 브라우저 자동 검증 기준 완료
  - Confirmed through browser automation

## 결론
## Conclusion

- `시간차(외출)` UI 1단인 `근무시간 기반 시작시간 옵션 제한`은 반영 완료됐습니다.
- Layer 1 for `시간차(외출)` UI restriction is complete.

- 다음 단계는 계획서 순서대로 `저장 직전 프런트 차단` 정리입니다.
- The next step is the `pre-save frontend guard` from the plan.
