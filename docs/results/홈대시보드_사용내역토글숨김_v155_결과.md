# 홈 대시보드 사용내역 토글 숨김 v155 결과
# Home Dashboard Usage Toggle Hidden v155 Result

## 요약
## Summary

- 홈화면 `팀원 연차 현황` 헤더에서 `사용 내역` 토글 스위치를 숨겼다.
- Hid the `Usage History` toggle switch from the `Team Member Leave Status` header on the home dashboard.

- 기능 자체는 삭제하지 않고, UI만 숨겼다.
- The functionality itself was not removed; only the UI was hidden.

## 반영 내용
## Applied Changes

- `ui_dashboard_v155_codex.js`에서 헤더 오른쪽 컨트롤 묶음에서 `사용 내역` 토글 렌더를 제거했다.
- Removed the `Usage History` toggle rendering from the right-side control group in `ui_dashboard_v155_codex.js`.

- `펼치기/접기` 버튼만 남도록 정리했다.
- Left only the `Expand/Collapse` button visible.

## 기대 결과
## Expected Result

- 홈화면 `팀원 연차 현황` 카드 헤더에는 `접기/펼치기` 버튼만 보여야 한다.
- Only the `Expand/Collapse` button should remain visible in the `Team Member Leave Status` card header on the home dashboard.

- `사용 내역` 토글은 화면에 더 이상 보이지 않아야 한다.
- The `Usage History` toggle should no longer be visible.
