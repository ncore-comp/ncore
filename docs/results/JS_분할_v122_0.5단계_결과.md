# JS 분할 v122 0.5단계 결과
# JS Split v122 Phase 0.5 Result

## 이번 단계 목적
## Purpose of This Phase

- 실제 함수 목록을 기준으로 `shell / shared / feature` 분류표를 확정합니다.
- Finalize the `shell / shared / feature` classification table from the actual function list.

## 분류 결론
## Classification Conclusion

### 1. shell 에 남길 축
### 1. What Stays in Shell

- 전역 상태값
- `init`
- 아주 얇은 진입점 성격의 함수

현재 기준 예:
- `init`
- 전역 state fields

주의:
- `renderNav`, `refreshDashboard`, `backToDashboard`, `changeMonth` 같은 함수는 자주 진입점처럼 보이지만 실제로는 여러 기능군을 건드립니다.
- 따라서 이 함수들은 shell 에 영구 고정하지 않고, 우선 `shared controller` 성격으로 유지합니다.

### 2. shared 로 묶을 축
### 2. What Belongs in Shared

실제 군집:
- `shared_core` 대역: 대략 `63 ~ 930`
- `shared_mid` 대역: 대략 `2270 ~ 3814`

대표 함수:
- 날짜/시간/텍스트
  - `fmtDate`
  - `formatDayCount`
  - `formatRequestPeriodText`
- 요청/상태/표시 공통
  - `getRequestDisplayType`
  - `getRequestStatusText`
  - `getRequestStatusClasses`
  - `shouldRenderRequestOnDate`
- 근무시간/주간 계산
  - `parseWorkShiftRange`
  - `getDailyActualWorkHoursForDate`
  - `getWeeklyWorkLimitSummary`
- 권한/범위 공통
  - `getCurrentPermissions`
  - `hasApprovalPermission`
  - `hasUserManagePermission`
  - `canManageTargetUser`
  - `canAccessAnyWorkReportView`
- 메일/세션/공통 라우팅
  - `getMailRecipientsForRequester`
  - `restoreSession`
  - `renderNav`
  - `refreshDashboard`
  - `backToDashboard`
  - `changeMonth`
- 공통 요청 모달
  - `openRequestModal`
  - `parseRequestTimeRange`
  - `findTimeOffConflictRequest`

판단:
- 이 축은 여러 기능군이 동시에 참조하므로, `phase 1~2` 동안은 분리하지 않고 공통 파일에 둡니다.
- These functions are used across multiple feature groups, so they should remain in a shared file during phases 1 and 2.

### 3. feature 로 확정된 축
### 3. Confirmed Feature Buckets

#### A. 상황판
#### A. Situation Board

- 라인 범위:
  - 대략 `945 ~ 1352`
- 핵심 함수:
  - `openSituationBoard`
  - `setSituationBoardScale`
  - `setSituationBoardDeptFilter`
  - `renderSituationBoard`
  - `renderSituationBoardSeatMapModalContent`
  - `renderSituationBoardDetailPanel`
  - `openSituationBoardSeatMap`

메모:
- `renderCalendarListView` 는 현재 이 대역 바로 뒤에 있지만 성격상 달력에 더 가깝습니다.
- 따라서 1단계 상황판 분리에서는 제외하고, 달력 단계에서 다룹니다.

#### B. 잔업/특근
#### B. Work Reports

- 라인 범위:
  - 대략 `3820 ~ 4727`
- 핵심 함수:
  - `openWorkReportApplyBoard`
  - `openWorkReportOverviewBoard`
  - `openWorkReportModal`
  - `submitWorkReport`
  - `getIntegratedReportBoardRows`
  - `getAdminReportBoardSummaryRows`
  - `getPendingWorkReportSettlementSummaries`
  - `approveCurrentWorkReportSettlement`
  - `rejectCurrentWorkReportSettlement`
  - `renderReportBoardModal`
  - `openReportUserDetail`

판단:
- 별도 화면/모달/정산 흐름이 이미 있으므로 2단계 분리 대상으로 적합합니다.

#### C. 구성원 관리
#### C. User Management

- 라인 범위:
  - 대략 `5595 ~ 5930`
- 핵심 함수:
  - `addUser`
  - `openUserEditModal`
  - `updateUser`
  - `deleteUser`
  - `renderUserSpecialLeaveEditor`

#### D. 대시보드 렌더
#### D. Dashboard Rendering

- 라인 범위:
  - 대략 `6033 ~ 6129`
- 핵심 함수:
  - `renderDashboardQuickActions`
  - `renderEmployeeDashboard`
  - `renderManagerDashboard`

#### E. 권한/설정
#### E. Permissions / Settings

- 라인 범위:
  - 대략 `6263 ~ 6938`
- 핵심 함수:
  - `buildSpecialLeaveTypeDraft`
  - `toggleSpecialLeaveSettings`
  - `collectSpecialLeaveTypesFromSettings`
  - `saveMasterSettings`
  - `saveMasterPermissions`
  - `setMasterPermissionTab`
  - `setPermissionColumnOpen`
  - `renderMasterPermissionPage`

## 경계가 애매한 함수
## Borderline Functions

아래 함수는 첫 분리 단계에서 억지로 옮기지 않습니다.
Do not force-move these in the first extraction step.

- `renderCalendarListView`
  - 위치는 상황판 근처지만 성격은 달력
- `refreshDashboard`
  - 대시보드 진입점이지만 거의 모든 화면 전환과 연결
- `changeMonth`
  - 달력/상황판/잔업특근 정산월이 함께 사용
- `openRequestModal`
  - 연차/시간차/운영실 대리등록 공통

## 1단계 분리 대상 확정
## Phase 1 Extraction Scope Confirmed

- 먼저 분리할 것은 `상황판`만입니다.
- The first extraction target is `Situation Board` only.

포함:
- `openSituationBoard`
- `setSituationBoardScale`
- `setSituationBoardDeptFilter`
- `setSituationBoardSelectedDate`
- `updateSituationBoardSelectedDateUI`
- `openSituationBoardInfoModal`
- `closeSituationBoardInfoModal`
- `getSituationBoardSeatMapPdfSrc`
- `getSituationBoardSeatAnnotations`
- `getSituationBoardSeatMapSeats`
- `getSituationBoardSeatStatusMap`
- `getSituationBoardSeatDailyAbsentGroups`
- `getSituationBoardSeatToneClass`
- `getSituationBoardSeatStatusLabel`
- `syncSituationBoardSeatMapPanelHeight`
- `renderSituationBoardSeatMapModalContent`
- `openSituationBoardSeatMap`
- `closeSituationBoardSeatMapModal`
- `getSituationBoardScaleConfig`
- `ensureSituationBoardSelectedDate`
- `getSituationBoardFilteredRequests`
- `getSituationBoardEventsForDate`
- `isSituationBoardLongAbsence`
- `buildSituationBoardMonthlyData`
- `getSituationBoardDailySummary`
- `getSituationBoardLongAbsenceTone`
- `renderSituationBoardLongAbsences`
- `renderSituationBoardDetailPanel`
- `renderSituationBoard`

제외:
- `renderCalendarListView`
- shared 권한/근무시간 유틸
- `changeMonth`

## 한 줄 결론
## One-line Conclusion

- `0.5단계` 기준으로 보면, 다음 실제 분할은 `상황판 전용 메서드 약 29개`를 `ui_situation_v122_codex.js` 로 옮기는 작업부터 시작하는 것이 가장 안전합니다.
- Based on phase 0.5, the safest next real extraction is to move about 29 Situation Board methods into `ui_situation_v122_codex.js`.
