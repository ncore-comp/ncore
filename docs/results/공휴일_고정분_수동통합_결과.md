# 공휴일 고정분 수동 통합 결과

작업 일시
- 2026-04-07

작업 목적
- `company_fixed`로 따로 관리되던 `노동절`, `창립기념일`을 `manual` 휴일 목록으로 통합
- 운영실 화면에서 `창립` 별도 집계가 아니라 `회사휴일`로 일관되게 보이도록 정리

적용 내용
- backend
  - `publicHolidaySyncService.js`
    - 회사 고정휴일 동기화 문서를 `source: "manual"`로 생성
    - `provider: "ncore-fixed"`, `category: "company_fixed"` 메타 추가
    - 재동기화 시 `company_fixed` legacy 문서와 `company-fixed-*` 문서를 함께 정리 후 재생성
  - `adminOpsService.js`
    - 운영실 `manualHolidays` 목록에 legacy `company_fixed`와 통합된 회사 고정휴일도 포함
    - 집계 `manual`에 회사 고정휴일까지 포함
    - 동기화 결과 응답에 `manualIntegratedCount` 추가
  - `holidayService.js`
    - holiday sanitize 시 `provider`, `category` 유지

- frontend
  - `ui_logic_new_design_v123_codex.js`
    - 공휴일 동기화 완료 팝업 문구
      - `창립기념일 n건` -> `회사 고정휴일(수동 통합) n건`
    - 운영실 휴일 패널 문구
      - `수동 휴일 설정` -> `회사 휴일 설정`
      - `창립` 배지 제거
      - `회사휴일` 배지로 통합
    - 고정휴일 편집 시 `company-fixed-YYYY-MM-DD` id를 유지하도록 보정
  - `core_logic_new_design_v123_codex.js`
    - `manualHolidays` sanitize 시 `provider`, `category` 유지

데이터 이관
- 백업 생성
  - `backups/2026-04-07_company-fixed-pre-merge/holidays-company-fixed.json`
- 이관 스크립트
  - `scripts/merge-company-fixed-into-manual.js`
- 이관 결과
  - 기존 `company_fixed` 10건을 `manual`로 전환
  - 전환 후 상태
    - `manual`: 10건
    - `company_fixed`: 0건
    - `integratedCompanyHolidays`: 10건

검증 결과
- 단독 함수 테스트 성공
  - `node --check`
    - `functions/src/services/holidayService.js`
    - `functions/src/services/adminOpsService.js`
    - `functions/src/services/publicHolidaySyncService.js`
    - `js/core_logic_new_design_v123_codex.js`
    - `js/ui_logic_new_design_v123_codex.js`
    - `scripts/merge-company-fixed-into-manual.js`

- 통합 경로 테스트 성공
  - `functions + hosting` 배포 완료
  - `sync_public_holidays(2026~2027)` 응답 확인
    - `autoCount: 42`
    - `companyFixedCount: 4`
    - `manualIntegratedCount: 4`
  - Firestore 재확인
    - `manual: 10`
    - `companyFixed: 0`
    - `integratedCompanyHolidays: 10`

- 실제 사용자 실행 경로
  - 운영실 휴일 패널 렌더 문자열 확인
    - `회사 휴일 설정` 문구 존재
    - `회사휴일` 배지 문구 존재
  - 브라우저에서 사용자가 직접 본 화면 확인은 아직 미실시

운영 반영
- 완료
- URL: https://ncore.web.app
