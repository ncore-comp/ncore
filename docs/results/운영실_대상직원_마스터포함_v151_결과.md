# 운영실 대상 직원 마스터 포함 v151 결과
# Admin Ops Target User Includes Master v151 Result

## 요약
## Summary

- 운영실 `대상 직원` 드롭다운에서 마스터가 빠지던 제한을 제거했다.
- Removed the restriction that excluded the master account from the Admin Ops `Target User` dropdown.

- 이제 운영실 접근 권한이 있는 사용자는 마스터도 대상 직원으로 선택할 수 있다.
- Users with Admin Ops access can now select the master account as a target user.

## 반영 내용
## Applied Changes

1. `ui_logic_new_design_v151_codex.js`
1. `ui_logic_new_design_v151_codex.js`

- `getAdminOpsTargetUsers()`에서 `u.role !== 'master'` 필터를 제거했다.
- Removed the `u.role !== 'master'` filter from `getAdminOpsTargetUsers()`.

2. `index.html`
2. `index.html`

- 운영 로드 버전을 `ui_logic_new_design_v151_codex.js`로 올렸다.
- Bumped the loaded version to `ui_logic_new_design_v151_codex.js`.

## 기대 결과
## Expected Result

- 운영실 `대상 직원` 목록에 마스터가 보여야 한다.
- The master account should appear in the Admin Ops `Target User` list.

- 선택 날짜 대리 등록도 마스터를 대상으로 열 수 있어야 한다.
- The `Register Proxy Request for Selected Date` flow should also be openable for the master account.
