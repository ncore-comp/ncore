# 파일 버전 정리 v001 결과 (2026-04-08)
# File Version Cleanup v001 Result (2026-04-08)

## 요약
## Summary

- 현재 운영 JS 파일명을 `v001` 세트로 재시작했다.
- Restarted the current production JS file naming as the `v001` set.

- 이전 `_vNNN_codex.js` 파일은 `js\legacy_versions`로 이동했다.
- Moved previous `_vNNN_codex.js` files into `js\legacy_versions`.

- 이후 작업은 `v002`, `v003`처럼 다시 올려가며 관리하면 된다.
- Future work can now continue with `v002`, `v003`, and so on.

## 실제 반영 내용
## Actual Applied Changes

### 1. 현재 운영본 복사
### 1. Copied Current Production Files

- 현재 운영 파일을 새 이름으로 복사:
- Copied the current production files under new names:
  - `*_v001_codex.js`

### 2. 운영 HTML 교체
### 2. Updated Production HTML

- `index.html`이 `v001` 세트만 읽도록 변경했다.
- Updated `index.html` so it only loads the `v001` set.

### 3. 과거 버전 분리
### 3. Separated Old Versions

- `js` 루트에는 현재 운영 `v001` 파일 `15개`만 남겼다.
- Left only `15` current production `v001` files in the `js` root.

- 기존 버전 파일 `155개`는 `js\legacy_versions`로 이동했다.
- Moved `155` existing versioned files into `js\legacy_versions`.

- 추가로 남아 있던 비표준 사본 파일 `ui_logic_new_design_v114_codex - 복사본.js`도 `legacy_versions`로 이동했다.
- Also moved the leftover nonstandard copy file `ui_logic_new_design_v114_codex - 복사본.js` into `legacy_versions`.

## 현재 상태
## Current State

- 루트 운영 파일 수: `15`
- Root production file count: `15`

- 레거시 보관 파일 수:
- Legacy archive count:
  - `_vNNN_codex.js`: `155`
  - 비표준 사본 포함 총 파일: `156`

## 관련 문서
## Related Documents

- 계획:
  - [파일버전네이밍_계획.md](/D:/MYDOC/Desktop/vscode/Web/firebase/ncore_web/ncore-main/docs/plans/파일버전네이밍_계획.md)

- 현재 운영 기준:
  - [현재운영_JS버전맵_v001_2026-04-08.md](/D:/MYDOC/Desktop/vscode/Web/firebase/ncore_web/ncore-main/docs/reference/현재운영_JS버전맵_v001_2026-04-08.md)

## 이후 작업 규칙
## Rule For Future Work

- 다음 수정부터는 실제 운영에 바뀌는 파일을 `v002`로 올린다.
- Starting with the next change, bump the actual production files to `v002`.

- 직전 운영 세트는 `legacy_versions`로 이동한다.
- Move the previous production set into `legacy_versions`.
