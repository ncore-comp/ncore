> 시간 기록 기준: Asia/Seoul
> 문서 최초 작성 시각(기록 기준): 2026-03-26 09:03:12
> 문서 최근 수정 시각(기록 기준): 2026-03-26 09:03:12

# 읽기 사용량 절감 결과

## 1. 정리한 원인

읽기 사용량이 많이 나오는 큰 원인은 아래 순서로 정리했다.

1. 백업 / 비교 / 검증 스크립트
2. 메인 데이터 로드
3. 운영실 진입 시 로그/휴일 로드
4. 접속 로그 CSV 전체 다운로드
5. Firebase Console 직접 조회

쉽게 비유하면:
- 창고 전체 재고조사(백업/비교)가 제일 크고,
- 매장 오픈 시 기본 장부 읽기(메인 로드),
- 경비실 최근 출입기록 확인(운영실),
- 출입장부 통째 복사(CSV 다운로드)
순서로 부담이 있다.

## 2. 이번에 바로 적용한 것

### 1) 테스트용 accessLogs 전체 삭제

실행 결과:
- 삭제 전: `2444건`
- 삭제 수: `2444건`
- 삭제 후: `0건`

이후 새 로그인으로 다시 생긴 현재 로그:
- `1건`

즉:
- 테스트 중 쌓인 출입장부는 모두 비웠고,
- 지금 남은 건 새로 발생한 최신 기록뿐이다.

### 2) 운영실 최근 로그 로드량 축소

기존:
- 서버 로드 `150건`
- 화면 표시 `20건`

변경:
- 서버 로드 `20건`
- 화면 표시 `20건`

즉:
- 보이는 만큼만 읽도록 줄였다.

## 3. 반영 파일

- [D:\MYDOC\Desktop\vscode\Web\firebase\ncore_web\ncore-main\functions\src\services\adminOpsService.js](/D:/MYDOC/Desktop/vscode/Web/firebase/ncore_web/ncore-main/functions/src/services/adminOpsService.js)

## 4. 배포

- `functions` 운영 반영 완료

## 5. 검증

운영 서버에서 `load_admin_ops_data` 호출 결과:

- `result: success`
- `accessLogsCount: 1`
- `holidaySummary`
  - `auto: 42`
  - `companyFixed: 10`
  - `manual: 7`

즉:
- 운영실 최근 로그는 현재 실제 데이터 기준으로 1건만 내려오고,
- 로드 제한도 정상 적용된 상태다.

## 6. 중요한 주의

- 이미 지나간 Firestore 사용량 그래프는 줄어들지 않는다.
- 이번 조치는 `앞으로 발생할 읽기량`을 줄이는 효과다.
- CSV 다운로드는 여전히 전체 로그를 읽는다.
  - 다만 로그 수 자체를 비웠기 때문에 당분간 부담이 크게 줄어든 상태다.

## 7. 결론

- 테스트용 접속 로그 정리 완료
- 운영실 최근 로그 로드량 20건 제한 완료
- 앞으로의 읽기 부담은 이전보다 줄어든 상태다.

## 8. 추가 판단 메모

- 현재 읽기 사용량 급증의 큰 원인은 `실사용자 업무`보다
  - 백업
  - compare
  - 검증 스크립트
  - 전체 로그 CSV 다운로드
  같은 관리/점검 작업이었다.
- 따라서 실사용이 들어간 뒤에는 지금처럼 큰 스파이크가 반복될 가능성은 낮다.
- 다만 장기적으로 사용자가 늘고 데이터가 많이 쌓이면,
  메인 로드 구조(`loadData`)는 최적화 검토 대상이 될 수 있다.

즉:
- 지금 같은 급격한 튐은 줄어들 가능성이 높고
- 향후에는 `장기 최적화`만 신경 쓰면 된다.
