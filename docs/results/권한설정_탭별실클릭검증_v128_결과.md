# 권한/설정 탭별 실클릭 검증 v128 결과
# Master Permissions Real Click Verification v128 Result

## 1. 검증 대상
## 1. Scope

- `권한 탭`
- `설정 테이블`
- `메일 설정`
- `잔업/특근 설정`
- `운영실`

## 2. 검증 방식
## 2. Verification Method

- 마스터 계정 로그인
- Log in with the master account

- `권한/설정` 페이지 진입
- Enter the `master permissions` page

- 탭을 순서대로 실제 클릭/전환
- Click and switch each tab in sequence

- `운영실`에서는 `선택 날짜로 대리 등록` 버튼까지 실제 클릭
- In `admin ops`, also click the `Register on selected date` button

## 3. 실행 스크립트
## 3. Executed Script

- [verify-master-permission-tabs-v128.js](/D:/MYDOC/Desktop/vscode/Web/firebase/ncore_web/ncore-main/scripts/verify-master-permission-tabs-v128.js)

## 4. 결과
## 4. Result

- 총 `5 passed / 0 failed`
- Total: `5 passed / 0 failed`

세부 결과:
Detailed results:

1. `권한 탭`
   - 통과
   - Passed
   - `마스터 권한 관리`, `권한 탭`, `게시판`, `세부설정` 확인
   - Confirmed `master permissions`, `permissions tab`, `board`, and `advanced settings`

2. `설정 테이블`
   - 통과
   - Passed
   - `직원 카드 기능`, `홈페이지 기능`, `게시판 기능` 확인
   - Confirmed `member card`, `homepage`, and `board` toggles

3. `메일 설정`
   - 통과
   - Passed
   - `메일 설정`, `수신인 (To)`, `참고인 (CC)` 확인
   - Confirmed `mail settings`, `To`, and `CC`

4. `잔업/특근 설정`
   - 통과
   - Passed
   - `잔업 신청 기능`, `특근 신청 기능`, 하단 `설정 저장` 버튼 확인
   - Confirmed overtime toggle, holiday-work toggle, and the bottom `Save Settings` button

5. `운영실`
   - 통과
   - Passed
   - `접속 로그 불러오기`, `보안 로그 불러오기`, `선택 날짜로 대리 등록` 확인
   - Confirmed `Load access logs`, `Load security logs`, and `Register on selected date`
   - `선택 날짜로 대리 등록` 클릭 후 모달이 실제로 열림
   - The modal actually opened after clicking `Register on selected date`
   - 모달 제목: `관리자 대리 등록`
   - Modal title: `관리자 proxy registration`

## 5. 산출 파일
## 5. Output File

- [verify-master-permission-tabs-v128-last.json](/D:/MYDOC/Desktop/vscode/Web/firebase/ncore_web/ncore-main/reports/verify-master-permission-tabs-v128-last.json)

## 6. 결론
## 6. Conclusion

- `v128` 기준으로 `권한/설정` 5개 탭의 실제 클릭 전환은 정상이다.
- Under `v128`, the actual click transitions for the five `master permissions` tabs are normal.

- 특히 `운영실 > 선택 날짜로 대리 등록`도 실제 클릭 경로에서 다시 동작한다.
- In particular, `admin ops > Register on selected date` is working again on the real click path.
