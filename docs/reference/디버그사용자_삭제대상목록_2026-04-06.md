# 디버그 사용자 삭제 대상 목록 (2026-04-06)
# Debug User Deletion Target List (2026-04-06)

## 목적
## Purpose

- 운영 사용자 목록에 섞여 들어온 `DBG...` 디버그 사용자를 정리합니다.
- Remove `DBG...` debug users that were accidentally mixed into the live user list.

## 원인 요약
## Cause Summary

- 이 문제는 기존 직원 이름이 바뀐 것이 아니라, 디버그용 임시 사용자가 운영 `users` 컬렉션에 새로 생성된 것입니다.
- The issue is not that existing employee names changed; temporary debug users were newly created in the live `users` collection.

- 생성 로그 기준:
- Based on access log records:
  - 생성 주체 / Created by: `Web관리자`
  - 생성 시각 / Created around: `2026-04-06 11:38 ~ 11:47`

## 삭제 기준
## Deletion Criteria

- `users.id`가 `DBG`로 시작하는 사용자
- Users whose `users.id` starts with `DBG`

## 삭제 대상
## Deletion Targets

1. `DBG_ANNUAL_debug_1775443102050`
   - 이름 / Name: `DBG_ANNUAL_debug_1775443102050`
   - 사번 / Employee No: `DBG_ANNUAL_debug_1775443102050`
   - 부서 / Dept: `????`

2. `DBG2_debug2_1775443210968`
   - 이름 / Name: `DBG2_debug2_1775443210968`
   - 사번 / Employee No: `DBG2_debug2_1775443210968`
   - 부서 / Dept: `????`

3. `DBG3_debug3_1775443279129`
   - 이름 / Name: `DBG3_debug3_1775443279129`
   - 사번 / Employee No: `DBG3_debug3_1775443279129`
   - 부서 / Dept: `????`

4. `DBG4_debug4_1775443496910`
   - 이름 / Name: `DBG4_debug4_1775443496910`
   - 사번 / Employee No: `DBG4_debug4_1775443496910`
   - 부서 / Dept: `????`

5. `DBG5_debug5_1775443593767`
   - 이름 / Name: `DBG5_debug5_1775443593767`
   - 사번 / Employee No: `DBG5_debug5_1775443593767`
   - 부서 / Dept: `????`

6. `DBG6_debug6_1775443632231`
   - 이름 / Name: `DBG6_debug6_1775443632231`
   - 사번 / Employee No: `DBG6_debug6_1775443632231`
   - 부서 / Dept: `????`

## 연관 데이터 점검 결과
## Related Data Check Result

- 요청 / Requests: `0건`
- 특별휴가 부여 / User special leave grants: `0건`
- 접속 로그 / Access logs: `13건`

설명 / Notes:
- 요청과 특별휴가 부여는 없어 사용자 문서만 삭제해도 업무 데이터 손실이 없습니다.
- 접속 로그는 감사 추적용이므로 이번 단계에서는 유지합니다.
- There are no requests or special-leave grants linked to these users, so deleting only the user documents does not lose business data.
- Access logs are retained for audit history in this step.

## 삭제 후 기대 결과
## Expected Result After Deletion

- 구성원 관리 목록에서 `DBG...` 사용자 6명이 사라집니다.
- 관리자 대리등록/선택 드롭다운에서 `DBG...` 이름이 사라집니다.
- Existing real employees remain unchanged.

