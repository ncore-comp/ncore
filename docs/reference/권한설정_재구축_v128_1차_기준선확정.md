# 권한/설정 재구축 v128 1차 기준선 확정
# Master Permissions Rebuild v128 Phase 1 Baseline Confirmation

## 1. 이번 단계에서 한 일
## 1. What Was Done in This Phase

- 현재 `v127` 권한/설정 관련 파일을 백업했다.
- Backed up the current `v127` permissions-related files.

- `v114 ~ v121` 후보 중에서 정상 한글 기준선 후보를 다시 점검했다.
- Rechecked the `v114 ~ v121` candidates to find a clean Korean baseline.

- `v121`의 권한/설정 블록을 실제 재구축 기준선으로 추출했다.
- Extracted the `v121` permissions block as the actual rebuild baseline.

## 2. 백업 위치
## 2. Backup Location

- [2026-04-07_permissions-rebuild-pre-v128](/D:/MYDOC/Desktop/vscode/Web/firebase/ncore_web/ncore-main/backups/2026-04-07_permissions-rebuild-pre-v128)

포함 파일:
Included files:
- `index.html`
- `ui_permissions_v127_codex.js`
- `ui_logic_new_design_v127_codex.js`
- `ui_shared_helpers_v127_codex.js`
- `ui_shared_request_v127_codex.js`

## 3. 기준선 결론
## 3. Baseline Conclusion

- 기준선 후보 중 `v121`이 가장 적절하다.
- `v121` is the most suitable baseline candidate.

이유:
Reasons:

1. `v121`은 한글 문자열이 실제로 살아 있다.
1. `v121` still contains intact Korean strings.

2. `권한/설정`, `설정 테이블`, `메일 설정`, `잔업/특근 설정`, `운영실` 구조가 모두 포함되어 있다.
2. It already contains the structure for `permissions`, `settings table`, `mail settings`, `workreport settings`, and `admin ops`.

3. 현재 `v127`보다 기능은 적어도, 재구축 기준선으로 쓰기에는 더 안전하다.
3. Even if it lacks some of the latest behavior, it is safer than the current `v127` as a rebuild baseline.

## 4. 추출한 기준선 범위
## 4. Extracted Baseline Range

- 원본 파일:
- Source file:
  - [ui_logic_new_design_v121_codex.js](/D:/MYDOC/Desktop/vscode/Web/firebase/ncore_web/ncore-main/js/ui_logic_new_design_v121_codex.js)

- 추출 범위:
- Extracted range:
  - line `6151 ~ 6785`

- 추출 결과 파일:
- Extracted output file:
  - [v121_permissions_lines_6151_6785.js](/D:/MYDOC/Desktop/vscode/Web/firebase/ncore_web/ncore-main/backups/2026-04-07_permissions-rebuild-pre-v128/v121_permissions_lines_6151_6785.js)

## 5. v121 기준선 위에 다시 얹어야 하는 최신 변경
## 5. Recent Changes That Must Be Reapplied on Top of v121

1. `잔업/특근 설정` 탭 하단 `설정 저장` 버튼 표시
1. Show the bottom `Save Settings` button on the `workreport settings` tab

2. 운영실 로그 자동 로드 제거
2. Remove automatic log loading in `admin ops`

3. 운영실 `접속 로그 불러오기 / 보안 로그 불러오기`
3. Preserve the `Load access logs / Load security logs` behavior

4. 운영실 `선택 날짜로 대리 등록` 경로
4. Preserve the `Register on selected date` flow

5. 게시판 읽기/쓰기 권한 그룹
5. Restore the `board read/write` permission group

## 6. 현재 판단
## 6. Current Assessment

- 재구축은 이제 실제로 시작된 상태다.
- The rebuild has now actually started.

- 아직 `v128` 소스 파일 생성은 안 했지만, 가장 중요한 준비 단계인 `정상 기준선 확보`는 끝났다.
- The `v128` source file has not been generated yet, but the most important preparation step, securing a clean baseline, is complete.

## 7. 다음 단계
## 7. Next Step

- `ui_permissions_v128_codex.js`를 `v121` 기준선 위에서 새로 세운다.
- Build `ui_permissions_v128_codex.js` anew from the `v121` baseline.

- 그 다음 최신 변경을 작은 묶음으로 재적용한다.
- Then reapply the recent required behavior in small bundles.
